\javaSource "methodExample/"; // location of class definitions

\programVariables {
  Person p;
  int x;
}

\problem {
      {eventNr:=0}
      {p.age:=2} // assign initial value to 'age'
        (   x = 0 & client != null
         -> \<{
                client.deliver(
              }\> (eventSeq(seqSingleton(\if(eventCon(event(p, Person#birthday, seqEmpty), TRUE))\then(TRUE)\else(FALSE)))))

}
