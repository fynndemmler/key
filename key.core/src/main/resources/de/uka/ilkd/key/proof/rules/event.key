\heuristicsDecl {
    simplify_event;
    simplify_eventseq;
}

\sorts {
    MethodId;
}

\predicates {
    /* Note: eventCons are nonrigid because they depend on the state of a program. */
    \nonRigid event(MethodId, boolean);
    \nonRigid eventSeq(Seq);
}
